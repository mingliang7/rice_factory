cashRoutes = FlowRouter.group({
  prefix: '/cash'
    //subscriptions: function (params, queryParams) {
    //    // Global subscriptions for route group
    //    this.register('sample_address', Meteor.subscribe('sample_address'));
    //}
});
