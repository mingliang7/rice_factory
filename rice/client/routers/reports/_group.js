riceRoutes = FlowRouter.group({
    prefix: '/rice'
    //subscriptions: function (params, queryParams) {
    //    // Global subscriptions for route group
    //    this.register('rice_address', Meteor.subscribe('rice_address'));
    //}
});
