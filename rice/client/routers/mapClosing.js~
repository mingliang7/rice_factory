riceRoutes.route('/mapClosing', {
  name: 'rice.mapClosing',
  action: function(params, queryParams) {
    Layout.main('rice_mapClosing');
  },
  breadcrumb: {
    //params: ['id'],
    //queryParams: ['show', 'color'],
    title: 'Map Closing',
    parent: 'rice.home'
  }
});
